# 2026-07-17 進捗

## 今日の方針

先輩から「開発範囲が広くなっている」と指摘があったため、実装を広げるよりも研究のコアを絞ることを優先する。

## 研究の中心

教師AIが生徒AIの内部状態を直接見ず、授業中に観察できる生徒反応だけをもとに、伝達AIがクラス状態を要約し、教師AIが授業構成を変えられるかを検証する。

## 追加・整理したもの

- `notebooks/paper_core_experiment.ipynb`
- `docs/paper_experiment_core.md`

`docs/paper_experiment_core.md` では、以下を整理する。

- 研究の問い
- 論文MVPの範囲
- 今はやらないこと
- 評価指標
- 優先順位

`notebooks/paper_core_experiment.ipynb` では、概要説明ではなく、論文に使う実験と対応ソースコードをまとめる。

- 使用するソースコード一覧
- データセットとして用意したクラスの確認
- 情報分離の確認
- クラス条件別の授業構成比較
- 論文用CSV/JSONの保存

## 優先順位

1. 論文用ノートブックを短く保つ
2. 情報分離を確認する
3. クラス条件別の授業構成比較を固定する
4. 伝達AIの要約を評価する
5. LLM化や長期授業は後回しにする

## 次にやること

`paper_core_experiment.ipynb` をColabで実行し、出力される比較表が論文の主実験として使えるか確認する。

## 追加: LLM伝達AIの確認

- `notebooks/paper_core_experiment.ipynb` に LLM communication AI bridge check セルを追加
- 観察可能イベントを入力し、LLM伝達AIが個人特徴とクラス要約を作る流れを確認できるようにした
- 伝達AIの出力を `TeacherBeliefManager` に渡し、教師側beliefから授業構成を生成する経路を接続した
- `src/observer/trait_classifier.py` のLLM向けプロンプトを読みやすいJSON指示に修正した
- LLM出力がJSONとして壊れた場合でも、ルールベース伝達AIへfallbackする

## 修正: Colab LLM依存関係

- `bitsandbytes>=0.46.1` を `requirements.txt` に明記
- `paper_core_experiment.ipynb` のColab setupセルで `pip install -r requirements.txt` を実行するようにした
- Transformersの `torch_dtype` 非推奨警告に対応し、`src/model_loader.py` では `dtype` を使うようにした
